- clean: ['~']
- shell:
    - [git submodule update --init --recursive, Installing submodules]
- link:
    ~/.bin: bin

    ~/.zgen: zgen
    ~/.zshrc: zshrc

    ~/.gitconfig: gitconfig

    ~/.config/nvim:
        path: config/nvim
        create: true
